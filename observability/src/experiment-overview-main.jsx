import React from "react";
import ReactDOM from "react-dom/client";
import ExperimentOverviewApp from "./ExperimentOverviewApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ExperimentOverviewApp />
  </React.StrictMode>
);
